# FIXTURE: invalid Kubernetes manifest used by scripts/verify.sh to prove kubeconform fires.
# `replicas` must be an integer, and `spec.selector` is required.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-invalid
spec:
  replicas: "three"
  template:
    spec:
      containers:
        - name: app
          image: nginx:latest
